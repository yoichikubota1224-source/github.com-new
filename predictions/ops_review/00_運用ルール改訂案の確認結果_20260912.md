# 運用ルール改訂案（統一運用プロンプト v1.0／本シート復旧・運用再設計案）の確認結果

状態：`PROPOSED_NOT_DEPLOYED` を維持。本書は確認結果であり、承認でも配備でもない。
`ACCURACY_SCORING=NO` / `RULE_PROMOTION=NONE` / `PURCHASE_ALLOWED=NO` / `SOURCE_DATA_MUTATION=NO` を継続。

受領3ファイルのうち、md 2件は**バイト同一**（同じ文書の重複）。実体は「復旧・運用再設計案」と「統一運用プロンプト v1.0」の2件。

---

## 1. 結論

**改訂の方向に異議はない。** 当方の現行実装と衝突する点は2件、裁定が必要な点は4件、v1.0に受け皿が無い未解決の定義裁定が4件。

v1.0 のうち、当方の現行運用と**一致していて変更不要**なもの：必須ゲートの順序（対象日・キー→AB→ROI指定範囲→CB→CJ→DA）、調教師運勢を必須ゲートへ戻さない扱い、ファクター順、結果照合で採点しない扱い。

---

## 2. 9/12の結果を当方の事前記録と照合した（事実のみ）

提示文書の結果表を、当方が発走前に凍結した台帳と突き合わせた。**1〜3着の21頭すべてで馬番と馬名が一致（不一致0）。** 数値の主張も2件とも一致した。

- 中山11R⑯の仕上指数67・順位14位、上位⑨の74・1位 → 当方台帳と一致
- 中山11R⑯の11:39時点15.8倍 → 当方の凍結記録と一致（当方の観測時の人気は8番人気、確定は5番人気）

事前フラグ別の着内・着外は件数のみ示す（`02` のCSV）。**単日・小標本のため率は出さず、ここからルールを昇格させない。**

| 事前フラグ | 該当 | 1〜3着 | 3着外 |
|---|---:|---:|---:|
| 危険区分 来ない疑い | 8 | 0 | 8 |
| 危険区分 過剰人気警戒 | 2 | 2 | 0 |
| 限定判定語 断念疑い | 14 | 0 | 14 |
| ROI救済（回収率TRUE） | 8 | 2 | 6 |

**内部矛盾の実例が1件出た。** 中山8R⑫コスモナエマは「過剰人気警戒」と「ウルトラTRUE(n=25)」が同一馬で同時に立っており、2着に入った。同レース⑤ヤマニンヘルシェもウルトラTRUE(n=25)で1着。**救済レイヤーと危険区分が同じ馬で衝突したときの扱いが、現行ルールにも v1.0 にも書かれていない。** 1日の結果で優先順を決めるべきではないが、衝突の表示方法は決めておく必要がある。

なお**結果ZIP（55ファイル）は当方未受領**である。着順・払戻は提示文書の表からのみ把握しており、払戻額・上がり・通過順・馬体重は原票で検算していない。

---

## 3. 当方の実装を直す必要がある点（2件）

### 要是正1　列名「独立バケット」が v1.0【5】【7】T09 と正面から衝突する

v1.0 は「支持数は独立根拠数ではない。同じ原票を複数AIが説明しても一つの出所として扱う」と定め、復旧案T09も「独立2根拠に加算しない」とする。
当方の台帳の列名は `支持系統数_独立バケット` / `不安材料数_独立バケット` である。当方は既に注記で独立性が未証明であることを書いているが、**列名そのものが禁止事項を主張している。**
→ 次版から `支持系統数_計上ベース` 等へ改める。既存台帳は凍結済みのため書き換えず、旧列名の対応表を添える。

### 要是正2　復旧案T03の `BLOCKED` を当方が持っていない

T03 は「一意に解けなければUNKNOWN、**別馬ならBLOCKED**。前方一致へ戻らない」とする。
当方のr5実装は前方一致を採らず結合不一致で停止する（`UNK_NAME_JOIN_KEY_MISMATCH` 等）ので前半は満たすが、返す状態が**UNKNOWN系のみで BLOCKED が無い**。
→ 別馬と判明した場合の `BLOCKED` を新設し、「解けない」と「別馬と判明」を分ける。

---

## 4. 裁定が必要な点（4件）

### 要確認1　必須ゲート第3段の中身（重要）

復旧案5節の実ファイル診断によれば、`RaceInput!AZ:BA` は `CourseROI_DB!J` の**評価マーク**（J4は○）を返す。実数ROIではない。
当方はこの第3段を「ROI指定範囲（AZ:BA等）」と呼び、**実測回収率のゲート**として扱ってきた。マークを返す版なら、**この段は実測ROIを供給できない。**
→ 「ROIゲート＝実測単複ROI（券種・条件・n・期間つき）」と「AZ:BA＝枠／脚質条件マーク」を別段にするか、第3段の参照先を `JockeyROI!J/K`（単回収率・複回収率）系へ差し替えるかの裁定をお願いしたい。当方の必須ゲート定義はこの裁定に従う。

### 要確認2　`RaceInput!E2` の照合失敗時の戻り値100

復旧案は「照合失敗時100を返す経路あり」と記す。ちょうど100が失敗の既定値なら、**E由来のROI=100は「実測100%」と「未取得」を区別できない。**
→ 本シート側で失敗時を空欄＋理由へ変えていただきたい。それまで当方は E由来のROI=100 を HOLD として扱う。

### 要確認3　当方の役割縮小を受け入れてよいか

v1.0 の ROLE=CLAUDE は返却項目を「主張ID/原材料/相反内容/確認した原位置/確認結果/統合へ影響する点/残る不足」とし、「抽出済み一覧の全面再作成を常態化させない」とする。
当方はこれまで**108頭×134列の全頭台帳とULMB判定台帳を主成果物**としてきた。v1.0 では全頭台帳は本シートの `01_判断材料` が担う設計であり、当方は相反解消に絞られる。
→ 役割縮小として受け入れてよいか裁定をお願いしたい。受け入れる場合、当方の台帳生成コードとULMB判定台帳は**成果物から入力へ**位置づけを変える。どちらでも実装できる。

### 要確認4　「1レース1枚」とレース横断の選定表

v1.0【7】は「主画面は対象1レースにつき一枚」とする。当方の現行成果物は7レース108頭を1つのCSVに束ねている。1レース1枚へ分割すると、**荒れ選定（テクニカル6帯の比較）の出力先が無くなる。**
→ レース横断の選定表を別画面として残すか、`00_運用状態` に含めるかの裁定をお願いしたい。

---

## 5. v1.0 に受け皿が無い未解決の定義裁定（4件）

v1.0【4】は未完項目に「欠ける実体／必要な原位置／担当／次の一手／解除条件」を書く欄を用意した。よい仕組みだが、**次の4件の裁定そのものは未解決のまま**で、裁定が出るまで該当条件は UNKNOWN が続く。

1. **K-01 コンピ指数2種の供給**（9/13分が未受領。テクニカル6を作れず荒れ選定の入口に入れない）
2. **K-02 減量記号→kg凡例**（★が1.0と4.0に割れている。9/13は減量条件が193行UNKNOWN）
3. **K-05 「5着以下／以内」「前走中央場所」の原典定義**
4. **K-06 父系の正本**

---

## 6. 今回の結果が改訂案の論点に与える裏付け

- **v1.0【7】「順位14位を自動的な不適性にしない」** — 中山11R⑯は仕上指数67・順位14位で3着。復旧案4節の指摘どおり、順位一つで評価を決める書き方は避けるべきだった。ただし1例であり、逆に「7点差は小さい」とも断定しない（復旧案の記述に同意）。
- **照合対象の設計** — ChatGPTの結果照合対象12頭のうち、中山11R④は競走除外、阪神11R⑬は競走中止で、**2頭は着順が存在しない**。照合対象を選ぶ時点で、結果が出ない可能性のある枠を織り込む必要がある。疾病中止を調教評価の裏付けにしない扱い（復旧案T10）に同意する。
- **同着** — 阪神11R⑤⑭が8着同着、中山3R⑨⑪が7着同着。今回はいずれも3着外で払戻の分岐は生じなかったが、払戻別表を複数行で持つ設計の必要性は変わらない。
- **直前更新** — 中山4R3着⑩は基準17番人気→確定8番人気の移動があった。v1.0【8】「直前値が取れないときは取得不能と表示し、昼のオッズや結果側の最終値を直前値と呼ばない」に同意する。

---

## 7. 当方の越権防止の確認

本書は買い目・最終印・軸・購入用組番・点数・資金配分を含まない。本採用・断念を確定していない。的中率・回収率は算出していない（件数のみ）。単日の結果でルールを昇格させていない。原資料・正本Vault・相手の成果物を変更していない。改訂案の受領を承認と扱っていない。

```text
PROMPT_VERSION_REVIEW = COMPLETED_NOT_APPROVED
DEPLOYED = NO
ACCURACY_SCORING = NO
RULE_PROMOTION = NONE
PURCHASE_ALLOWED = NO
SOURCE_DATA_MUTATION = NO
STATUS = SHADOW_ONLY
```
