# 本シート整合性確認と現行必要ファクターの抜け漏れ是正 2026-09-07

```text
受領物 = 本シート_Claude対応・抜け漏れ監査_20260907.zip
         SHA-256 dc36573e3b5a5e38042b71cb4e9bbfd2176f5476c5474276e582da71ba5bf12a
         同梱SHA256SUMS 7/7 OK(SHA256SUMS.txt自身を除く全ファイル)
利用者指示 = 「本シート不要は私の指示です。しかし現行必要ファクターの抜け漏れ確認是正の上、
              運営していただきたい」「アップデートはOK」「添付資料はCodexにてobsidian照合」
本書の位置づけ = Codexの確認依頼6問への回答＋抜け漏れの特定＋是正案
統制 = RACE_GATE_STATUS=BLOCKED / PURCHASE_STATUS=PROHIBITED / RULE_PROMOTION=NONE
       MIGRATION_APPROVAL=NOT_GRANTED / SOURCE_DATA_MUTATION=NO
       最終印・軸・買い目・点数・資金配分・精度採点なし。相関係数・一致率は計測していない
```

---

## 1. 【最重要】抜け漏れの正体は、ファイルではなく構造です

先に結論を書きます。**探すべき欠落ファイルはありません。宙吊りになっているのは工程の構造です。**

### 1-1. 何が起きているか

現行の必須ゲートは6項目です。その供給元を棚卸しすると:

| 必須ゲート | 供給元 | 本シートを外した後の状態 |
|---|---|---|
| ① 運勢(騎手AB) | **騎手運勢xlsx**(本シートとは別ファイル) | **供給あり**。9/12-13分は生成済み |
| ② 回収率/ROI(AZ:BA) | 本シート `RaceInput!AZ / BA` | **供給元が消える** |
| ③ CB | 本シート `RaceInput!CB` | **供給元が消える** |
| ④ CJ | 本シート `RaceInput!CJ` | **供給元が消える** |
| ⑤ DA(ズレ時DB) | 本シート `RaceInput!DA / DB` | **供給元が消える** |
| ⑥ 本シート判定 | 本シートそのもの | **供給元が消える** |

**6項目中5項目が本シート由来です。** 本シートを外すと、ゲートは規約上生きたまま供給元だけが消えるので、`race_gate_status` は**恒久的にBLOCKED**になります。当工程の実測がそれを裏づけています。

> 8/16工程の全頭スクリーン478行で、**CB=`[不足]`×478 / CJ=`[不足]`×478 / DA=`[不足]`×478**。ROIは `騎手ROI参考101[RaceInput未照合]` のような参考値のみ。運勢も `△(天王星−)[参考:未照合]`。**正式選定0R・正式穴軸0頭**で終わっています。

Codexの監査も同じ状態を独立に確認しています(M09/M10/M11 = `HOLD_NOT_RECEIVED`、M18 = 本シート不使用は移行完了・同等性の記録ではない)。

### 1-2. 指示は矛盾していません ― 3つを分ければ解けます

ChatGPT確認結果の修正文Aが、既にこの3つを分けるよう求めています。

| | 内容 | 羊一様の指示 |
|---|---|---|
| (a) **日常操作の休止** | 本シートExcelを毎週開いて入力・再計算しない | **これが「本シート不要」** |
| (b) **論理ゲートの維持** | AB→AZ:BA→CB→CJ→DA の確認順とゲート自体は残す | 廃止の指示は出ていない |
| (c) **参照実装の移行** | ゲートの供給元を別実装へ置き換える | `MIGRATION_APPROVAL=NOT_GRANTED` |

**「本シートを操作しない」と「本シートの値を受け取らない」は別です。** 今は後者まで一緒に止まっているため、(b)が空回りしています。

### 1-3. 是正の選択肢(裁定は羊一様)

| 案 | 内容 | 評価 |
|---|---|---|
| **A. 値だけ軽量CSVで受け取る** | 本シートは開かない。CB/CJ/DA/DB/AZ/BAの**保存値だけ**をキー・時点・出典つきCSVで受領。Codex `02_軽量CSV受渡し項目仕様.csv`(59列・PROPOSED)がその仕様案 | **推奨**。(a)を守ったまま(b)が回る。本シート全冊のアップロードも不要になり、労力削減の方針とも整合 |
| B. ゲート定義を改訂 | CB/CJ/DAを必須から外す | 定義変更。**外した後の判断根拠を別途定義する必要**があり、今は代替が無い |
| C. 同等実装へ移行承認 | 別実装でCB/CJ/DAを再現 | 同等性検証が前提。移行マッピングv1.3は**全11行PROPOSED**で未検証 |
| D. 現状維持 | 何もしない | **恒久BLOCKED**。正式選定が永久に0Rのまま |

**Aは「本シート不要」の指示と衝突しません。** 週に一度、必要な列だけのCSVを1枚受け取る形です。

---

## 2. Codexの確認依頼6問への回答

> Codexは「現物のファイル名・版・SHA256・列名・対象日・件数で」「未受領なら未受領、未実装なら未実装と」回答するよう求めています。装わずに答えます。

**大前提**: Codexが参照している9/6工程の資料はすべて `C:/Users/User/Desktop/...` にあり、**本セッションは1件も受領していません**。当リポジトリ(`yoichikubota1224-source/github.com-new`)が保持する予想工程の実体は **8/16分が最後**です。したがって多くが「未受領」になります。

| # | 質問 | 回答 |
|---|---|---|
| **1** | 当日実際に読んだROI正本・集計期間・取得日・結合数・未結合理由。実ROI/卍/統合指数を別々に | **未受領**。当工程はROI正本を1件も受領していません。8/16工程で持っていたのは**騎手運勢xlsx内に併記された騎手ROI参考値のみ**(478行すべて `騎手ROI参考NNN[RaceInput未照合]`)で、集計期間・母数・主体・条件は**いずれも無し**。実ROI/卍/統合指数の区別は、当工程側の資産(`細分化ROI卍_活用方法検討案_20260818.md`)では**分離済みの設計**ですが、**実データの受領は0件**です |
| **2** | AB / AZ:BA / CB・CJ・DA・DB を何から受領し、どこで計算・照合したか | **AB**: 受領あり(騎手運勢xlsx)。ただし**RaceInput未照合**で、8/16は `[参考:未照合]` 表記のまま。**AZ:BA**: 未受領。**CB/CJ/DA/DB**: **未受領**(列は出力に存在するが478/478が`[不足]`)。**当工程はClaudeの評価を本シート値として埋めていません。** 限定判定語(`VALUE_H1_HOLE_AXIS_RECHECK(PROV)` 等)と本シート列は別列で保持しています |
| **3** | 旧条件の馬場状態・回り・内外を21_以外のどの資産で補うか | **補完していません。不足のままです。** 当工程が保持する `細分化ROI卍_活用方法検討案_20260818.md` §1 の被覆マトリクスでも、**良重(馬場状態)は層3に軸が無く層2限定**、しかも種牡馬・調教師は「道悪一括」で重と稍重を区別できないと記録済み。**回り・内外は当工程の資産に軸自体がありません**。Codex M07の指摘と一致します |
| **4** | 9/6新聞の新規8列・外厩4列のv2取込先・検算・実利用列・意図的非利用 | **未受領・未確認**。9/6工程は当セッションの範囲外です。`build906 v2` の実体も見ていません |
| **5** | 当日運勢の未結合・候補・競合、T-15実売オッズとTB時点、対象馬集合の同一性 | **9/6分は未受領**。ただし**同種の問題を8/16分で実測済み**です: (i) 運勢の名寄せ競合は6件、うち**正式名の完全一致は3件のみ**(森田誠也・今村聖奈・ディー)で、残り3件は短縮名。**自動ツールのマスタも正解扱いしない**方針を確定済み(`穴馬抽出マニュアルv1.1 §C/§D`)。(ii) **T-15は未取得**。8/16の市場系は全478行が `基準/前日20260815T1143Z` 固定。(iii) 対象馬集合の同一性はDE照合で478頭一致・真の騎手変更0件を確認済み |
| **6** | 旧受渡しCSVで省略される日付・racekey・DA根拠DB・原本SHA・時点をどの明細で保持するか | **当工程の現行出力では、日付・racekey・馬番は保持していますが、DB・原本SHA・snapshot_id・rule_version は保持していません**(§3の被覆表)。§4で是正案を出します |

### 2-1. Codexの前提のうち、当工程の記録と食い違う点

Codexは「9/6のClaudeは本シートCB/CJ/DAの列を保有していない」と記録していますが、**当リポジトリの8/16工程では列自体は出力に存在し、値が `[不足]` です**。「列が無い」のではなく「列はあるが供給が無い」状態です。監査上の意味は同じですが、是正の設計が変わります(列を作る必要はなく、供給を繋げばよい)。

---

## 3. 現行必要ファクターの抜け漏れ確認(02仕様59列 × 当工程の実測)

Codexの `02_軽量CSV受渡し項目仕様.csv`(59列・全て `PROPOSED_NOT_IMPLEMENTED`)を、当工程の現行出力と突合しました。

| 状態 | 件数 | 内容 |
|---|---:|---|
| **保持** | 4 | `race_date` `racekey` `horse_no` `horse_name` |
| **部分保持(1列に混在。分離が必要)** | 8 | `jockey_unsei`＋`_status`(→ `fortune` 列に `△(天王星−)[参考:未照合]` として値・群・状態が同居) / `roi_win_pct`系(→ `roi` 列に `騎手ROI参考101[RaceInput未照合]`) / `odds_kind`＋`odds_asof`＋`odds_value`(→ `odds_asof` 列に `10.7(基準/前日20260815T1143Z)`) / `claude_assessment`(→ `value_grade`) |
| **列はあるが値が無い** | 3 | `main_CB` `main_CJ` `main_DA`(478/478が`[不足]`) |
| **無い** | 44 | 下記 |

### 3-1. 無い44列の内訳 ― 3つの塊に分かれます

| 塊 | 列 | 取得の難易 |
|---|---|---|
| **(i) 来歴・時点・版** | `input_snapshot_id` `source_manifest_id` `comparison_asof` `calculation_rule_version` `main_source_file/sha256/sheet/row` `claude_output_id/sha256/asof` | **当工程だけで付与できる。外部データ不要** |
| **(ii) 本シート由来の値** | `main_DB` `main_CI` `main_CZ` `frame_roi_AZ` `pace_roi_BA` ＋各`_status` | **本シートからしか来ない**(§1-3の裁定待ち) |
| **(iii) ROIの主体・条件・母数** | `roi_source_manifest_id` `roi_data_start/end/asof` `roi_join_status` `roi_n` `roi_entity` `roi_condition_key` `manji_*` `integrated_*` | **ROI正本の受領が前提** |
| その他 | `jockey_key` `trainer_key` `sire_key` `tb_asof` `tb_source_ref` `claude_rank`系 `comparison_status` `difference_reason` | 一部は当工程で付与可(騎手キーは入力CSVに実在) |

**要点: 44列のうち約10列は当工程だけで今すぐ埋められます。** 残りは供給の裁定(§1-3)かROI正本の受領が前提です。

---

## 4. 是正案 ― 当工程側で今すぐ直せるもの

裁定を待たずに実行できる分です。**アップデートOKの指示に基づき、次回実行分から適用する仕様として起案します**(8/16の過去成果物は歴史記録なので書き換えません)。

### 4-1. 出力列の分離(混在の解消)

| 現行 | 是正後 | 理由 |
|---|---|---|
| `fortune` = `△(天王星−)[参考:未照合]` | `jockey_unsei` / `jockey_unsei_group` / `jockey_unsei_status` / `jockey_unsei_source_ref` | 値・群・照合状態・出典を機械可読に分ける。Codex 02仕様と同名にする |
| `roi` = `騎手ROI参考101[RaceInput未照合]` | `roi_value` / `roi_kind`(実ROI/卍/統合/参考) / `roi_entity` / `roi_n` / `roi_join_status` | **実ROIと参考値の混同を構造的に防ぐ**(M05) |
| `odds_asof` = `10.7(基準/前日20260815T1143Z)` | `odds_value` / `odds_kind` / `odds_asof` | 独立レビューH-4の是正でもある |
| `ul_mb` = `[不足]\|激印3` | `ultra_mastbuy_status` / `gekiin_tags` | 未取得と補助タグの混同を防ぐ |

### 4-2. 来歴列の新設(外部データ不要)

`input_snapshot_id` / `source_manifest_id` / `calculation_rule_version` / `claude_output_id` / `claude_output_asof` / `claude_output_sha256` / `comparison_asof`。8/16工程は既に入力5CSVのSHA-256と保存時刻を `01_input_audit` に持っているので、**それを行レベルへ降ろすだけ**です。

### 4-3. 判定列の母集団を揃える(独立レビュー H-1/H-2/H-5 の是正)

これは既出の指摘ですが、02仕様の `missing_fields` / `intentionally_unused_fields` / `comparison_status` と同じ思想です。

- `fatal_flags` を**選定前に全478行へ付与**(現行 0/478)
- `hole_axis_eligibility` を**穴帯全頭へ付与**(現行 191頭中21頭のみ)
- 中穴帯にも役割判定列を付与(現行 16頭中0頭)

### 4-4. 騎手キーの追加(荒れ傾向CSV監査 D-1 との合流)

`03_value_horse_screen` に**騎手列がありません**(独立レビューH-6で「競合騎手騎乗の候補を判別できない」と記録済み)。加えて、本日受領した荒れ傾向分析CSVの監査で **騎手名がJVキャッシュでは最長4字の略称、Web取得ではフルネーム**と判明しました。

→ **`jockey_key`(正規化済み)と `jockey_name_raw`(原表記)を分けて持つ**。正規化規則の版も `calculation_rule_version` に含める。これで運勢の名寄せ・騎手ROIの結合・結果CSVとの突合が同じキーで通ります。

---

## 5. 本体への申し送り

| # | 事項 | 判定 |
|---|---|---|
| 1 | **供給元の裁定が最優先**。本シートを外した結果、必須ゲート6項目中5項目が供給元を失い、`race_gate_status` が恒久BLOCKEDになっている。§1-3のA〜Dから選ぶ必要がある | **人間確認必須**(羊一様の裁定事項) |
| 2 | **推奨はA案(軽量CSVで値だけ受領)**。本シートを開かない指示と衝突せず、全冊アップロードも不要。Codex `02_軽量CSV受渡し項目仕様.csv` 59列がそのまま仕様案になる | **本体で本採用再判定** |
| 3 | ROI正本は**当工程で1件も受領していない**。8/16で使えたのは運勢xlsx併記の参考値のみで、集計期間・母数・主体・条件は無し。実ROI/卍/統合指数の**分離設計は当工程にあるが、実データが無い** | **未取得** |
| 4 | 馬場状態・回り・内外の3軸は**補完していない**。良重は層2限定・道悪一括で、回り・内外は軸自体が無い(Codex M07と一致) | **未取得**(既起票の`良重_馬場状態_粒度向上_整備要件`が該当) |
| 5 | 9/6工程の資料は**本セッション未受領**。Q4・Q5の9/6分は回答不能。ただし同種の問題は8/16分で実測済み(運勢競合6件/完全一致3件、T-15未取得) | **未受領** |
| 6 | §4の是正(列の分離・来歴付与・判定列の母集団・騎手キー)は**外部データ不要で当工程だけで実行できる**。アップデートOKの指示に基づき次回実行分から適用する | **取得済**(実装は次回工程) |
| 7 | 相関係数・判定一致率は**計測していない**。同日同馬の本シート出力が無く、共通入力の一致を予想の同等性として報告しない(Codex M20と同意見) | **未計測**(意図的) |
| 8 | Codexの「9/6のClaudeはCB/CJ/DA列を保有していない」は、当工程では「列はあるが478/478が`[不足]`」。是正設計が変わる(列作成は不要、供給の接続のみ) | **要確認**(記述の擦り合わせ) |

---

## 注記

- 本書は**整合性確認と是正案**であり、レース予想ではない。`LIVE_PREDICTION=NOT_AUTHORIZED` / `race_gate_status=BLOCKED` / `purchase_status=PROHIBITED` を維持。
- **移行承認は行っていない。** `MIGRATION_APPROVAL=NOT_GRANTED` / `PRODUCTION_REFERENCE_SWITCH=PROHIBITED` は不変。§1-3の選択肢はいずれも**提示であって決定ではない**。
- **Claudeの評価を本シート値(CB/CJ/DA)へ代入していない。** 限定判定語と本シート列は別列で保持する設計を維持する。
- **未取得値を0や推定値へ置換していない。** `[不足]` は `[不足]` のまま。
- 意図的な保留(次走期待馬・調教師運勢・旧抽出タグの非復活)は**漏れに数えていない**(Codex M19と同意見)。
- 最終印・軸・買い目・点数・資金配分・最終採用馬・精度採点は含まない。
- 受領物への書き込みは0件。リポジトリ側の原本・Codex成果物・ChatGPT成果物・AGENTS.md・CLAUDE.md・00_必読・.obsidian も変更していない。過去成果物(8/16工程のCSV)も書き換えていない。
- 添付資料のObsidian照合はCodex側で実施済みとの申し送りを受領。**当セッションではObsidianを参照していない**(アクセス手段を持たない)。
