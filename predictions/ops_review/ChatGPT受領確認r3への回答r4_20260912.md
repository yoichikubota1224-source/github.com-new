# ChatGPT r3受領確認への回答 r4 — 検証と是正の記録

対象日 2026-09-12 ／ run_id `r4-20260912-manual` ／ 台帳版ID `r4`

```text
AUTHORIZED_PHASE = PHASE_1_PROCESS_REHEARSAL / STATUS = SHADOW_ONLY / RULE_PROMOTION = NONE
LIVE_PREDICTION = NOT_AUTHORIZED / PURCHASE_ALLOWED = NO / PURCHASE_STATUS = PROHIBITED
SOURCE_DATA_MUTATION = NO / ACCURACY_SCORING = NO
```

このファイルは**手順と検査の記録**です。指数の原数値・回収率の実数・ルール条件文・入力の指紋は含めません（それらは私用の引渡しパックにのみ置きます）。最終印・軸・買い目・馬番の組合せ・点数・資金配分・最終採否は**確定していません**。

## ChatGPTが要求した3ブロック

| ブロック | 指摘 | r4での是正 |
|---|---|---|
| A | 版差の「FALSE」と「非掲載」が混ざり、集約FALSEが「(行なし)」と表示されていた | 集約判定と馬別表掲載を**別列**に分離。変更ログを版差表から機械生成し、手書きの記述ずれを除去 |
| B | 生成コードに古い必須ゲート定義が残り、生成JSONへ出力されていた | 必須ゲートの**唯一の定義**（順序リスト＋構築関数＋混入検査の assert）を置き、生成直後にも検査 |
| B | 氏名照合に前方一致が残り、空欄どうしがTRUEになった | 空欄と短すぎる前方一致を**UNKNOWN**へ。判定と理由コードを1対1に |
| B | 生成スクリプトが固定ローカルパスと未同梱JSONに依存 | 辞書CSVを正式入力にし、環境変数で差し替え可能に。未同梱JSONへの依存を削除 |
| B | 生成コードが参照する判定台帳の版が古いまま固定されていた | 同じ版のディレクトリから読むよう変更。再実行後の台帳は**バイト同一**で数値は動かない |
| C | 列対応表で18列が「改名継続」と「新規」に二重分類されていた | **宛先1列＝1行**に再構成し、種別を continue/rename/split/merge/add/drop で明示 |

## 表示の是正

- 運勢の意味列は、対象セルの未照合中は**HOLD**にし、原記号は変更せず別列で保持する。
- 「独立バケット」は旧版からの**互換名**である旨と、グループ間の独立性が**未証明**である旨を全行に注記する。

## 検査結果

| 検査 | 対象 | 結果 |
|---|---|---|
| ChatGPT提供の追加回帰検査（4項目） | r4の実生成物と生成コード | **4/4 PASS** |
| `scripts/mainline_tests.py`（18項目） | 本線7Rの全頭評価台帳 | **18/18 PASS** |
| `scripts/ledger_tests.py`（22項目） | ULMB条件台帳 | **22/22 PASS** |

引渡しパックを展開したディレクトリに対しても同じ3本が通ることを確認しました。

### r4で見つかった**テスト側**の欠陥2件

テストが通っていても、テスト自体が間違っていれば意味がないため記録します。

| ID | 欠陥 | 是正 |
|---|---|---|
| M03 | 版IDを前版の文字列で**固定比較**していたため、版を上げると必ず落ちる | 成果物のファイル名から版を取り、全行が同一かつ名前と一致することを検査 |
| T07 | 減量記号の供給状況を**必須ゲート側から読んでいた**。Bブロックの是正でその項目を別の状態表へ移したため空振りしていた | 両方を見るように変更（旧版のログも読める） |

### 外部再実行の入力を確定

「22項目テストの外部再実行に必要な入力が足りない」という指摘に対し、必要な入力を**6点**に特定しました。うち4点（条件台帳・馬別台帳・適用範囲ログ・ゲート状態）は引渡しパックに同梱し、残り2点（出走表と過去1年の全レース全頭CSV）は既配布の原本を使う形にしました。あわせてテスト側の入力読み込みを、パックの連番付きファイル名でもそのまま読むよう変更しています。

**当方が実行したことを、相手方が再実行したことにはしません。**

## 未取得のまま残していること

当日馬場とトラックバイアス／展開・隊列・ペースの確定／上がり水準の閾値／コース形状と内外の凡例／休み明けの閾値／装備の装着有無／定義未確認の特記9語／特性48語と今回条件の結びつき／追切の原時計と短評／出遅率の分母／減量記号からkg数への凡例／必須ゲート2〜6段／回収率の集計期間と使用可否の原典定義／ラップキャラ空欄の凡例。

いずれも**推定で埋めていません**。未取得はUNKNOWNまたはHOLDのまま残し、確認済系統が1のため限定判定語は「ワイド穴として照合」までに留めています。

**自己採点は受入判定・精度評価・購入許可には用いません。**
