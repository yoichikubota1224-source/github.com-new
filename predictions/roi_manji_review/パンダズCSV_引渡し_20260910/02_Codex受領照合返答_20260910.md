# Codex受領照合返答｜パンダズCSV

2026年9月10日　Claude（独立再評価）→ Codex

様式: `Codex指示書_20260910.md` §8

```text
package_id：PANDAS_CSV_20260910_v1
受領したZIP／ファイル：パンダズCSV_Claude引渡し_20260910_v1.zip（789,327 bytes）
  ＋ パンダズCSV_Claude引渡し報告_20260910_v1.md（3,871 bytes）
実際に開けたファイル：ZIP内 49/49（04_manifest.csv 掲載48件＋manifest自身）。読めなかったファイル 0件
実際に計算したハッシュ照合結果：
  外側ZIP  sha256 = b4f860d7efd4dcc01d8028069ea18f1fd5ff7ee0874877e65befb595a9f9d258
           → 引渡し報告の記載値と64桁一致（bytes も一致）
  manifest 48件を1件ずつ再計算 → 一致48 / 不一致0 / 欠落0（bytes も全件一致）
  manifest 未掲載の実ファイルは 04_manifest.csv 自身のみ（自己非包含）
01_roi.csvの論理データ行数：1,222（expected 1,222）
騎手／種牡馬／調教師の行数：403 / 390 / 429（expected 403/390/429）
  ※ entity_type は JOCKEY / SIRE / TRAINER
ROI記入あり／未記入の件数：468 / 754（expected 468/754）
  内訳 JOCKEY 191/212・SIRE 113/277・TRAINER 164/265
  判定は win_roi_pct または place_roi_pct が非空。n_starts の充足も同一集合の468行
原行IDの重複・欠落：0 / 0
  record_id 1,222件すべて一意・空欄0
  source_row_no は source_id 別に 1-403 / 1-390 / 1-429 で欠番0・重複0
未解析・方式不明・時点不明の件数：0 / 1,222 / 1,222
  parse_status=PARSED 1,222（UNPARSED 0）
  roi_method=UNKNOWN 1,222 / popularity_timing=UNKNOWN 1,222
  period_start・period_end はともに全1,222行空欄
  entity_match_status=NAME_KEY_ONLY_UNVERIFIED_OFFICIAL_ID 1,222（公式ID未確認も全行）
  condition_parse_status=PARTIAL 91 / aggregation_status=UNVERIFIED_AGGREGATION 311
不足ファイル・読めない列：なし
PACKAGE_INTEGRITY：PASS（パック内部整合のみ。上流原本との同一性は未検証）
SCOPE_COVERAGE：PASS（04_manifest.csv 掲載範囲について。参考CSVの列対応は別判定）
  ※追補: 原本3CSVと 01_roi.csv の内容同一性を当方で検証済み（下記）。行数一致に留まらない
DATA_USABILITY：HOLD
CLAUDE_RECEIPT：VERIFIED（件数・ハッシュ・原行IDについて。全1,222行を実ファイルから集計。
  receipt_samples.csv の目視には依拠していない）
RACE_GATE_STATUS = BLOCKED (HOLD_UNVERIFIED)
RULE_PROMOTION = NONE
PURCHASE_ALLOWED = NO
```

**`CLAUDE_PROJECT_REGISTRATION` は UNVERIFIED です。** 当方はClaude Codeのリモート実行環境で稼働しており、Project登録画面を確認する手段を持ちません。したがって `HANDOFF_STATUS = NOT_READY` を維持します。

**`PACKAGE_INTEGRITY` の根拠範囲を限定した理由**: 照合相手の `04_manifest.csv` は同一ビルド（`scripts/finish_pack.py`）が生成した自己申告値です。48/48一致はパック内部整合の証明に留まり、上流原本との同一性は示しません。加えて `evidence/validation.json` 自身が **`manifest_checked: false`** と申告しており、`evidence/package_status.json` の `PACKAGE_INTEGRITY=PASS` と `evidence/build_summary.json` の `AWAITING_FINAL_VERIFICATION` は**互いに矛盾**しています。

---

## Codexへの依頼（当方が実施していない検査 8件）

`Codex指示書_20260910.md` §1 の担当区分（Codex＝入力棚卸し・ハッシュ・構造・欠損・重複・変更差分の監査）に照らし、**当方の担当外または実施不能**だったものを列挙します。当方が「自己申告の受け売り」に留まっている箇所です。

| # | 依頼内容 | 対象 | なぜ必要か |
|---|---|---|---|
| ~~1~~ | ~~`evidence/original/*.csv` 6件を `SHA256SUMS.txt` と突合~~ → **当方で実施済み。一致6/不一致0/欠落0**。`original_internal_hash_checks` 6件PASSを独立に再現 | — | **解消** |
| **2** | 入力ZIP（`evidence/input/20260819_…卍指数CSV保存.zip`）を読み取り専用で展開し、内部6メンバのハッシュを `ORIGINAL_ZIP_INTERNAL_HASH` 監査6行と突合 | 同ZIP | 監査6行の裏付けが未検証。**原本は変更しないこと** |
| **3** | `input_snapshot.json` の凍結入力 **24件** × `04_manifest.csv` の `sha256` を全件突合（当方は5件のみ実施、**残19件未実施**） | 両ファイル | 入力の同一性が部分確認に留まる |
| **4** | `evidence/validation.json` の **70件**チェックと `evidence/boundary_tests.json` の **8件**PASSを再実行。あわせて §6-2 の回帰6ケース（欠損対0／引用符入り原文／先頭ゼロID／重複行／曖昧な名前／未知のROI方式）を8テストが覆っているかを点検 | 両ファイル＋`scripts/` | 当方は `logical_row_order`・`all_original_columns_values`・`raw_header_and_value_roundtrip_rows=1222` 相当を**独立に再現済み**（§下段）。残るのは70件・8件の網羅性と、**同一入力・同一コードでの再生成一致（バイト/内容）** |
| **5** | `scripts/pandas_pack.py` `finish_pack.py` `collect_web.py` を精読し、`record_id` 生成規則（`rec_ + SHA256(入力SHA + ":" + 元論理行番号)`）と欠損保持ロジックの**実装一致**を確認 | `scripts/` | 規則は README に記載があるが、**実装との一致は未確認** |
| ~~6~~ | ~~原本3CSVと 01_roi.csv の内容同一性~~ → **当方で実施済み。§下段のとおり無損失を確認** | — | **解消** |
| **7** | `02_sources.csv` 165行の `source_id` 一意性・`parent_source_id` 連鎖・`logical_rows` 整合・`raw_headers_json` の妥当性 | `02_sources.csv` | 当方は通読と `source_role` の集計のみ |
| **8** | `03_audit.csv` 6,148行の `audit_id` 一意性・`record_id` 外部キー整合・status語彙統制 | `03_audit.csv` | 当方は `audit_type` 18分類と status 分布（HOLD 6,128 / PASS 19 / PENDING 1）の集計のみ |

| **9** | **変更差分・基準版の監査**（§1担当区分・§4-4） | `package_status.json` / ファイル名日付 | `baseline_status=INITIAL_BASELINE_NO_PRIOR_VERSION_DIFF` の妥当性、ファイル名日付 20260701／20260719 と収録ZIP 20260819 の版関係、`_pre実測_backup` が示す別版の存在（**未取得・要確認**）、次回差分キーの定義。**現状どの担当にも割り当てられていません** |
| **10** | **重複の実測値検証**（§4-4 の6キー: 対象ID・条件・期間・券種・購入方式・出典） | `build_summary.json` | `exact_duplicate_rows=0` / `exact_duplicate_groups=0` / `potential_duplicate_rows=0` が**自己申告のまま**。再計算をお願いします |
| **11** | **参考資料の区分監査**（§2-2 の主対象／参照／旧版／対象外と対象外理由）と `REFERENCE_STRUCTURE=HOLD_8_SHORT_ROWS` の解消 | 調教タイム基礎定義CSV | 当方は列数不一致8行を再現済みだが、**列対応の確定は未実施**。なお本返答の「不足ファイル・読めない列：なし」は**主データ3表についての判定**で、参考CSVの列対応は別判定です |

### 追加で新設をご検討いただきたい列

| 列 | 理由 | 実測 |
|---|---|---|
| **重複群キー**（エンティティ単位） | `duplicate_group_id`・`potential_duplicate_group_id` が**ともに 0/1,222 充足**で、同一エンティティの反復加点を機械的に止める列が無い | ROI充足468行の**201エンティティ中107（53%）が2行以上**。最厳格層143行でも88エンティティ中28が複数行 |
| **母集団抽出フラグ** | `raw_record_json` の `リスト` 列に TOP10 630 / ワースト3 189 があり、**819行は原表で上位下位のみ抽出済み**。うち542行（66.2%）は `n_starts` 欠落 | 母集団代表値として扱えないことを、CSV上のフラグで明示したい |
| **集計の被覆キー**（場/距離/回りの重複識別） | 場集計・距離集計・回り集計は同一のコース別表 `cidx_e` を切り直したもので、**同じ出走を重複して含む** | 3切り口が揃う21エンティティで Σn が19件不一致＝各行は特定値の部分集合 |

---

## 当方が追加で実施した検証（依頼#1・#6 の解消）

**Codex指示書 §6-1「全件の行き先が説明できること」§6-2「必須の構造検証」に相当する検査を、当方が実施しました。**

| 検査 | 方法 | 結果 |
|---|---|---|
| **原本3CSVとの内容同一性** | `evidence/original/` の3ファイルを直接読み、各行の `raw_record_json` の `headers`／`values` と**全行・全列を突き合わせ** | **ヘッダ不一致 0 / 値不一致 0**。403+390+429 = 1,222行すべてで無損失。**行数一致に留まらず内容一致を確認** |
| `source_row_no` の対応 | 原本の物理行順と突合 | 3表すべて 1..N の連番で**原本の行順どおり** |
| **原本6件のSHA-256** | `evidence/original/SHA256SUMS.txt` と実ファイルを再計算 | **一致6 / 不一致0 / 欠落0**。`original_internal_hash_checks` 6件PASSを独立に再現 |
| **欠損表現の写像** | 原本の `単回収率(%)`・`複回収率(%)`・`件数(n)` の各セル原文と 01_roi.csv の3列を全1,222行で突合 | **値が一致しない行 0/1,222**。原本側にも `'0'` が18・空欄が754と**別に存在**し、`0` と欠損の分離は**原本レベルで確認**。空欄754行すべてに `MISSING_ROI`、値0の18行すべて理由空欄 |
| **確認集合**（§8本文の要求） | 3表 × 先頭／末尾／欠損／保留／複雑文字列 の15件を当方の規則で選定し、同梱 `receipt_samples.csv` と突合 | **record_id 一致 9/15**。差異6件は `FIRST_HOLD` と `COMPLEX_RAW` の**選択規則の違い**で、同梱側は3つの理由に同一の record_id（各表の先頭行）を割り当てています。当方は `aggregation_status=UNVERIFIED_AGGREGATION` の初出行と `raw_record_json` 最長行を選びました |

**この結果 `SCOPE_COVERAGE` の意味が変わります。** 初版では「行数一致は内容一致ではない」と留保しましたが、**内容同一性は確認できました**。残る未実施は §依頼4・5・7・8・9・10・11 です。

### record_id を特定した指摘（§8本文の要求）

| record_id | field_name | 指摘 |
|---|---|---|
| `rec_130853df77b6d2f1baa837c84501c02e244c8d5afffb5e57efd35a2f96ddea7b` | `win_roi_pct` | 値 `0` かつ `place_roi_pct` は非0、`n_starts=3`。`roi_missing_reason` 空欄。**この `0` が実測値か、原表側の未取得表現かを原本の掲載形から確定してください**（原本側も `'0'`） |
| `rec_b8343ede7c363687566f91dfb220478a1fbcbaef9e4f86c0856db8066b7a21cb` | `win_roi_pct` / `place_roi_pct` | **両方が `0`** で `n_starts=18`。同上 |
| `rec_cb1b1ae83c0bc7fa2dfcb341eb95e691baa19a2464e208b2d03202270a80e094` | `n_starts` | `n_starts=2` で両ROIが `0`。**n=2 の行にROI値が付く意味**（分母2での0%か、未取得か）の確定をお願いします |

18行すべての record_id は成果物 `03_出典定義監査_20260910.csv` で `win_roi_pct_present=Y` かつ値0の行として特定できます（値そのものは未収録・§公開範囲の裁定待ち）。

## 当方の再計算で確定した派生値（正本ではありません）

| 値 | 導出 | 注意 |
|---|---|---|
| 合算留保 **311 = 距離等249 + クラス62** | `condition_json` の `kind`（NAMED_CONDITION 204 / JOCKEY_COLUMNS 107）と `03_audit.csv` の `reason_code=CLASS_SCOPE_COLLAPSE` 24件から再構成 | **CSVに直接ラベルは存在しません。** 正本値として引用しないでください |
| `numeric_zero_cells` **25** | win 18 ＋ place 7 ＋ n_starts 0 | `build_summary.json` の記載値と一致。**行の和集合は18行** |
| ガードレール三層 **468→387→370→143** | ROI充足 → n≥30 → +条件PARSED → +DIRECT_TABLE_CLAIM | **監査対象母集団であって採用母集団ではありません。** 143行でも方式・時点・公式IDは全行未確定 |
| 妙味度と実測ROIの Spearman | 複勝 +0.6436（n≥30側 +0.6711）／単勝 +0.3857 | **探索的記述に留まります。** 母集団の468行は原表の TOP系387行＋ワースト系81行の**両極リストのみ**で、分布中央の行を含みません（抽出が説明変数=妙味度の順位側にかかっています）。TOP系のみでは複勝 +0.4987、騎手TOP群159行では複勝 +0.2649・単勝 +0.0713。**予測力・独立性の根拠にはなりません** |
| ~~妙味度と件数nの −0.4282~~ | **撤回** | 群間で**符号が反転**します（TOP系 −0.5370／ワースト系 **+0.4250**）。方向の異なる2つの抽出規則を混ぜた合成物（Simpson型）で、**データの性質ではありません** |
| 有意性・信頼区間 | **n=468 で計算してはいけません** | 実体は**201エンティティ**（1エンティティ最大13行）で、同一エンティティ内の条件は同じ出走を分割した重複母集団。`03_audit.csv` に `audit_type=INDEPENDENCE` が `status=HOLD`／`SOURCE_INDEPENDENCE_UNVERIFIED` で存在し、`next_action` は重複出所の独立加算禁止。1エンティティ1行に落とすと精度は**約2.3倍の過大評価** |
| 「n≥30層」の呼称 | **層ではなく閾値** | 残存率 82.7%(387/468)。閾値に単調でない（≥10:0.6776／≥20:0.6530／≥30:0.6711／≥50:0.6588／≥100:0.7052） |

## 統治

読み取り・静的照合のみ。受領ZIPは展開しただけで一切書き換えていません。原本、Codex成果物、ChatGPT成果物、AGENTS.md、CLAUDE.md、00_必読、.obsidian も未変更。`SOURCE_DATA_MUTATION = NO`。ROI原数値（名前と回収率の組）は本書・成果物CSV・内部作業記録のいずれにも保持していません。db-keiba への当方からのアクセスも行っておらず、サイト仕様はすべて同梱 `evidence/web/*.json` の観測記録の引用です。最終印・軸・買い目・点数・資金配分・購入可否・精度採点は出していません。
