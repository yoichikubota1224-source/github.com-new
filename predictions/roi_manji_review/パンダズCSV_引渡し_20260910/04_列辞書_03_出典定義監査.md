# 列辞書｜`03_出典定義監査_20260910.csv`

2026年9月10日　Claude（独立再評価）作成／Codex 9/10 確認結果の依頼により追加

対象: `03_出典定義監査_20260910.csv`（1,222行×52列。**1行 = 1条件**）
文字コード: **UTF-8 BOM付き**（同パック内の統一。ルート `MANIFEST.csv` はBOMなし。配布版の次回作成時に規約を揃えます）

---

## 0. 最初に読む注意 ── 母集団が列によって異なります

**この表には母集団の異なる列が同居しています。** 取り違えると件数が合いません。

| 母集団 | 行数 | 実体数 | 1実体の最大行数 | この母集団で計算されている列 |
|---|---:|---:|---:|---|
| **全行** | 1,222 | **347** | **21** | `entity_row_repeat_count`、`duplicate_group_id`、`potential_duplicate_group_id` |
| **ROI記入行**（`win_roi_pct_present=Y`） | 468 | **201** | **13** | 文書側（`02_Codex受領照合返答` の独立性・有効標本の記述）が使う母集団 |

`entity_row_repeat_count` を **ROI記入468行の反復件数と読むと 468行中344行で値が合いません**（実測）。
`02_Codex受領照合返答_20260910.md` が「実体は201エンティティ・1エンティティ最大13行」と書いているのは
**ROI記入468行を母集団としたとき**の値です。両者は別の数字で、どちらも正しく、混用が誤りです。

---

## 1. 識別・出所

| 列 | 意味 | 充足 | 注意 |
|---|---|---|---|
| `record_id` | 原本行の一意ID | 1,222/1,222・全件一意 | 原本 `01_roi.csv` の `record_id` と**同一集合**。原本照合の唯一のキー |
| `source_id` | 出典表ID | 1,222/1,222 | 出典表側で165件・全件一意、親参照の欠落・循環0 |
| `original_filename` | 原本CSVのファイル名 | 1,222/1,222 | 3表（騎手403／種牡馬390／調教師429） |
| `source_row_no` | 原本内の論理行番号 | 1,222/1,222・欠番0重複0 | 原本の**物理行順と一致**（当方で全行照合済） |
| `entity_type` / `entity_name_raw` | 対象種別／名前（原文） | 1,222/1,222 | **実体の同定はこの2列の組**。公式IDでの照合ではありません |
| `entity_id` / `entity_match_status` | 公式IDと照合状態 | — | **公式IDは全行未確認**。名前一致は同名別人を排除しません |
| `evidence_path` / `evidence_response_sha256` | 証跡の保存先とハッシュ | — | パック内 `evidence/web/*` の観測記録 |
| `evidence_source_id` | 行単位で辿れる公開表ID | **非空 10/1,222** | **単なる `source_url` の記載と区別**。初版で「157行追跡可」と書いたのは誤り（訂正A） |

## 2. ROI（値は未収録）

| 列 | 意味 | 充足 | 注意 |
|---|---|---|---|
| `win_roi_pct_present` / `place_roi_pct_present` | 単勝／複勝ROIの**有無のみ** | Y 468 / N 754 | **値も0値フラグも収録していません。** `Y` の中から値0の行（18行）を選び出すことは**この表だけではできません**（原本との `record_id` 照合が必要／訂正H） |
| `roi_values_withheld` | 値を伏せた理由 | `YES_PUBLIC_SCOPE_UNRULED` 468 / `NOT_APPLICABLE` 754 | 公開範囲が未裁定のため |
| `roi_method` | 購入方式 | **`UNKNOWN` 1,222** | 均等購入かオッズ反比例か不明。**最優先の未裁定事項** |
| `roi_method_basis` / `roi_method_options_on_site` | 方式の根拠／サイト上の選択肢 | — | サイトの**現行**説明は取得当時の設定を証明しません |
| `n_starts` / `n_races` | 件数 | — | **空欄は「0」ではなく[不足]**。n≥30 は 387/468 |
| `stake_total` / `return_total` | 賭け金総額／払戻総額 | **0/1,222** | パック内に存在せず。311行の再集計に必須で、これが無いため復元不能 |
| `aggregation_method` / `aggregation_status` | 合算方法／状態 | — | 311行は `Σ(ROI_i×n_i)/Σn_i` の整数丸め（**均等購入前提の式**） |
| `aggregation_code_source_id` / `rounding` | 合算コードの出所／丸め | — | 掲載値が既に整数%で、加重平均後に再度丸める**二重丸め** |

## 3. 条件・期間・人気

| 列 | 意味 | 充足 | 注意 |
|---|---|---|---|
| `condition_text` / `condition_kind` / `condition_parse_status` | 条件の原文・区分・解析状態 | PARSED 370/387（n≥30内） | 原文に「合算」の語があるのは**62行のみ**。249行は「集計」 |
| `course` / `surface_inout` / `kaisai_nissu` | コース／芝ダート内外／開催日数 | — | 場・距離・回りの3切り口は**同一のコース別表 `cidx_e` 由来**で同じ出走を重複して含む |
| `period_start` / `period_end` | 集計期間 | **0/1,222** | 全行空欄。`evidence/web/public_tables.csv` の公開表題に「（2021-2025）」の5年集計あり（**候補であって確定ではない**） |
| `observed_at` / `page_updated_at` / `displayed_data_asof` / `asof_consistency` | 取得時刻・ページ更新日・表示基準日 | — | 表示基準日がページ間で不一致（トップ 2026-08-16／使い方 2026-05-17） |
| `popularity_min` / `popularity_max` | 人気帯 | **0/1,222（全行空欄）** | **人気帯で絞る手段がパック内に存在しません**。Q0〜Q3 が着手不可な理由 |
| `popularity_timing` | 人気の基準時点 | **`UNKNOWN` 1,222** | 同上 |

## 4. 独立性・重複

| 列 | 意味 | 充足 | 注意 |
|---|---|---|---|
| `entity_row_repeat_count` | 同一実体（`entity_type`＋`entity_name_raw`）の**反復行数** | 1,222/1,222 | **母集団は全1,222行**（347実体・最大21行）。**ROI記入468行での反復件数ではありません**（§0） |
| `duplicate_group_id` / `potential_duplicate_group_id` | 重複群ID | **ともに 0/1,222** | パック側の現行定義（`source_id`＋`raw_record_json` の完全一致、およびコード由来の潜在キー）では検出0。**条件違いの同一実体の反復を否定する結果ではありません** |
| `source_family` / `source_family_note` | 出所系統 | DB_KEIBA 468 / BOOK_OR_UNSPECIFIED 754 | **`roi_presence_raw` と1対1**で、ROI記入の有無の言い換え。**「由来」の断定は撤回済**（訂正C） |
| `population_extraction` | 抽出規則 | — | 468行は原表の **TOP系387＋ワースト系81 の両極リストのみ**で分布中央を含みません。相関・有意性の解釈に直接影響します |

## 5. 状態・次手

| 列 | 意味 | 注意 |
|---|---|---|
| `metric_type` | 指標種別 | — |
| `missing_reason` | 欠損理由 | 原本側で `'0'` 18・空欄 754 が**別に存在**。`0` と欠損の分離は原本レベルで確認済 |
| `status` | 行の状態 | `DIRECT_TABLE_CLAIM_UNVERIFIED` 157行を含む。**集計・合算の観点で検証済みの行は0行**（311+157＝468全数が未検証／訂正G） |
| `next_action` | 次の手 | 重複出所の独立加算禁止を含む |

---

## 6. この表を使うときの禁止事項

1. `win_roi_pct_present=Y` を **ROI値の代わりに使わない**（値も0値も入っていません）
2. `entity_row_repeat_count` を **ROI記入468行の反復件数として読まない**（§0）
3. 空欄の `n_starts` を **0として扱わない**（[不足]です）
4. `duplicate_group_id=0` を **独立性の確認済みと読まない**
5. **n=468 を独立標本数として有意性・信頼区間を計算しない**（DEFF ≈ 2.22/2.41、有効標本 ≈ 211/194／訂正I）
6. 場・距離・回りの3行を **独立な3つの裏付けとして数えない**（同一のコース別表由来）
7. サイトの**現行**購入方式を、過去CSVの方式として**遡及設定しない**
8. この表由来の情報から **ワイド・3連複の組合せ確率やEVへ変換しない**（`DATA_USABILITY=HOLD`）
9. **買い目・最終印・資金配分・最終採用馬・本採用/断念の確定を出さない**（本体の裁定事項）
