# 00e Obsidian展開確認 追補f — 荒れレース選定の再設計(21)の登録照合

`親ファイル: 00b_Obsidian展開確認_追補c_20260823.md` / `RULE_PROMOTION=NONE` / `PURCHASE_ALLOWED=NO`

fileIdをキーに照合。**バイト完全一致・異本衝突0件。**

| ファイル | Drive fileId | bytes | SHA-256 | 照合 |
|---|---|---:|---|---|
| 21_荒れレース選定の再設計_20260823.md | `1XUL2ouQ-8Gbgr8T2kO_ZthU-z-dRHK91` | 8,493 | `9a2e2ef5a3cc1050…` | **一致** |

登録先フォルダ: `1R6V5VFgKYDPyGXNfT8QU9rVW2IoY2iGc`(追補b〜eと同一)。
同名の既存ファイルは無く、新規登録。追補方式のため既存ファイルの上書きは発生していない。

## 照合手順(再現可能)

1. `create_file`(parentId固定・`disableConversionToGoogleType=true`・`text/markdown`)
2. 返却された **fileId** で `download_file_content` → base64をローカルへ復号
3. `sha256sum` と `cmp` の両方でローカル正本と突合

⚠ ファイル名は一意識別子ではないため、照合は必ず **fileId** を主キーとする。

## 未登録のまま残るもの

| 対象 | 理由 |
|---|---|
| `scripts/are_grade_up.py` 等の解析スクリプト | コードのためGitHub正本のみ |
| `data/are/荒れ類型ラベル_1年3308R_20260802.csv` | Drive側が正本(取得元がDrive) |

## 統治

```text
ANALYSIS_SCOPE   ：POST_RACE_RESEARCH_ONLY
PURCHASE_ALLOWED ：NO
RULE_PROMOTION   ：NONE
異本衝突         ：0件
```

追補fは登録の照合記録であり、印・買い目・点数・資金配分・購入判断を含まない。
